const settings = () => {
    const settingsString = /* html */ `
        <p>Settings</p>
    `;

    const onRender = (settings) => {

    }

    return [settingsString, onRender];
};

export default settings;